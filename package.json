{
  "name": "companion",
  "version": "0.1.0",
  "description": "A small always-on-top overlay that brings animated Companions to your AI desktop app. Ships with Pane, the SVG companion from PaneStreet, as the reference pack.",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "install:app": "bash scripts/install-app.sh",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "wdio run wdio.conf.ts"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1",
    "@vitest/ui": "^2.1.8",
    "@wdio/cli": "^9.4.0",
    "@wdio/local-runner": "^9.4.0",
    "@wdio/mocha-framework": "^9.4.0",
    "@wdio/spec-reporter": "^9.4.0",
    "jsdom": "^25.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "webdriverio": "^9.4.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1"
  }
}
